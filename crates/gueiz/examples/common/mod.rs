//! 例で使う共通の道具。
//!
//! 検証用の例はどれもオフスクリーンに描いて画素を読み戻すので、
//! そのままでは**何も見えません**。ここに「描いた結果を窓に出す」仕掛けを置きます。
//!
//! 既定は今までどおり画面を出しません（すぐ終わるので、確認や CI に都合がよい）。
//! `--window` を付けたときだけ窓が開きます。
//!
//! ```sh
//! cargo run -p gueiz --example text_test              # これまでどおり
//! cargo run -p gueiz --example text_test -- --window  # 結果を目で見る
//! ```

pub mod preview;
